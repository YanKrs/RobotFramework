comandos: 
	pip install robotframework
	pip install robotframework-seleniumlibrary




Selenium library keywords, site - biblioteca que contem todas as palavras chaves do selenium



Para localizar um item através do Xpath é a seguinte sintaxe: 

//tipoDoElemento[@atributo="xxx"]
(//a[@id="video-title"])[1]   : Seleciona o primeiro elemento com aquele parametro, ou a posição que desejar informando no colchetes



Switch de teste é o nome dado ao conjunto de vários cenários de testes

Como rodar apenas um cenário de teste?: Rodar o comando no terminal robot -d pastaDosResultados -t "Digite o cenário"